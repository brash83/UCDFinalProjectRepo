import { defineConfig } from 'astro/config';

export default defineConfig({
  buildOptions: {
    site: 'https://brash83.github.io/UCDFinalProjectRepo',
    out: './dist',  // Set output folder to 'dist'
  },
});
